# 07 Transition Videos

Generate short clips from approved first/last frames.

Put each attempt in `attempts/`.

Move the best clips to `approved/`.

Approval gate:

> Does this clip move correctly from first frame to last frame?
